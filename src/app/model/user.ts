export class User{
   
    name: String;
    id: String;
    sex: String;
    emailAddress: String;
    // password field removed — never store passwords on client model
    motto: String;
    webPageUrl: String ;
}